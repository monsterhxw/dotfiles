# Alias

# Go to dirs
alias godl='cd ~/Downloads'
alias gopj='cd ~/project'
alias godt='cd ~/Desktop'
alias gh='cd ~'
alias gtmp='cd $HOME/project/tmp'

# Replace remove 
alias rm='trash'

# Replace ll and ls
#alias ll='ls -lhas'
alias ls='lsd -NAv'
alias ll='lsd -Nlav'
alias lt='lsd --tree -Nlv --depth 2'

# brew upgrade with xargs Commanda
alias brewupgrade='brew outdated | xargs -L 1 -p brew upgrade'

alias alacritty='/Applications/Alacritty.app/Contents/MacOS/alacritty'

alias tksv='tmux kill-server'

# AeroSpace Switch windows
alias ass='aerospace list-windows --workspace focused | grep -v "^$(aerospace list-windows --focused --format '%{window-id}')" | fzf --reverse | cut -f1 -d"|" | xargs aerospace focus --window-id'
